<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.JackZ.BestForum.mapper.manual.PostMapper">
    <resultMap id="BaseResultMap" type="com.JackZ.BestForum.model.Post">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Mon Oct 12 16:28:07 CST 2020.
        -->
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="title" jdbcType="VARCHAR" property="title" />
        <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
        <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
        <result column="creator_id" jdbcType="BIGINT" property="creatorId" />
        <result column="comment_count" jdbcType="INTEGER" property="commentCount" />
        <result column="like_count" jdbcType="INTEGER" property="likeCount" />
        <result column="view_count" jdbcType="INTEGER" property="viewCount" />
        <result column="gmt_latest_comment" jdbcType="BIGINT" property="gmtLatestComment" />
        <result column="status" jdbcType="INTEGER" property="status" />
        <result column="column" jdbcType="INTEGER" property="column" />
        <result column="permission" jdbcType="INTEGER" property="permission" />
        <result column="tags" jdbcType="VARCHAR" property="tags" />
    </resultMap>

    <sql id="Example_Where_Clause">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu Sep 05 06:46:42 CST 2019.
        -->
        <where>
            <foreach collection="oredCriteria" item="criteria" separator="or">
                <if test="criteria.valid">
                    <trim prefix="(" prefixOverrides="and" suffix=")">
                        <foreach collection="criteria.criteria" item="criterion">
                            <choose>
                                <when test="criterion.noValue">
                                    and ${criterion.condition}
                                </when>
                                <when test="criterion.singleValue">
                                    and ${criterion.condition} #{criterion.value}
                                </when>
                                <when test="criterion.betweenValue">
                                    and ${criterion.condition} #{criterion.value} and #{criterion.secondValue}
                                </when>
                                <when test="criterion.listValue">
                                    and ${criterion.condition}
                                    <foreach close=")" collection="criterion.value" item="listItem" open="(" separator=",">
                                        #{listItem}
                                    </foreach>
                                </when>
                            </choose>
                        </foreach>
                    </trim>
                </if>
            </foreach>
        </where>
    </sql>

    <update id="increaseCount" parameterType="com.JackZ.BestForum.model.Post">
        UPDATE post
        <set>
            <if test="viewCount != null">
                view_count = view_count + #{viewCount,jdbcType=INTEGER},
            </if>
            <if test="commentCount != null">
                comment_count = comment_count + #{commentCount,jdbcType=INTEGER},
            </if>
            <if test="likeCount != null">
                like_count = like_count + #{likeCount,jdbcType=INTEGER},
            </if>
        </set>
            where id = #{id}
    </update>

    <select id="countBySearch" parameterType="com.JackZ.BestForum.dto.PostQueryDTO" resultType="java.lang.Integer">
        SELECT COUNT(id)
        FROM post
        <where>
            <if test="search != null and search != ''">
                AND title REGEXP #{search}
            </if>
            <if test="tags != null and tags != ''">
                AND tag REGEXP #{tag}
            </if>
            <if test="time != null and time != ''">
                AMD gmt_create > #{time}
            </if>
            <if test="sort != null and sort != ' and sort == 'no'">
                AND comment_count = 0
            </if>
        </where>
    </select>

    <select id="selectByPost" parameterType="com.JackZ.BestForum.dto.PostQueryDTO" resultMap="BaseResultMap">
        SELECT *
        FROM post
        <where>
            <if test="search != null and search != ''">
                AND title REGEXP #{search}
            </if>
            <if test="tags != null and tags != ''">
                AND tag REGEXP #{tag}
            </if>
            <if test="time != null and time != ''">
                AMD gmt_create > #{time}
            </if>
            <if test="sort != null and sort != '' and sort == 'no'">
                AND comment_count = 0
            </if>
        </where>
        <if test="sort == null or sort == ''">
            ORDER BY gmt_create DESC
        </if>
        <if test="sort != null and sort != '' and sort == 'new'">
            ORDER BY gmt_create DESC
        </if>
        <if test="sort != null and sort != '' and (sort =='HOT7' or sort == 'HOT30' or sort == 'HOT')">
            ORDER BY comment_count DESC
        </if>
        LIMIT #{page},#{size}
    </select>
    
    <select id="selectByExampleWithRowBound" parameterType="com.JackZ.BestForum.model.PostExample" resultMap="BaseResultMap">
        SELECT * FROM post
        <if test="_parameter != null">
            <include refid="Example_Where_Clause"></include>
        </if>
        ORDER BY gmt_create DESC
    </select>

    <select id="selectRelated" parameterType="com.JackZ.BestForum.model.Post" resultMap="BaseResultMap">
        SELECT * FROM post
        <where>
            <if test="tags != null and tags != ''">
                AND tags REGEXP #{tags}
            </if>
            <if test="id != null and id != ">
                AND creator_id = #{creatorId}
            </if>
        </where>
        ORDER BY gmt_latest_comment DESC
        LIMIT 20
    </select>



</mapper>

